import React from 'react';

const Greeting = () => {
  const message = 'Halo, selamat datang di aplikasi React!'

  return (
    <div>
      <h1>{message}</h1>
    </div>
  )
};

export default Greeting;